ribbon:  
  ReadTimeout: 60000  
  ConnectTimeout: 60000 
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 16000
zuul:
  debug:
    request: true
  ignored-services: service-one
  prefix: /api
  routes:
    baidu:  
      path: /baidu/**
      url: unieap-hystrix-member